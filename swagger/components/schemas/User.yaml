type: object
properties:
  id:
    type: string
  email:
    type: string
    format: email
  subscription:
    type: string
    enum: [starter, pro, business]
    default: starter
required:
  - email
  - subscription